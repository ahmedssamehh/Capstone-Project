# Injected by CI/CD from vault / secret manager — never commit secrets.env
# For local render/validation only:
#   cp secrets.env.example secrets.env
DATABASE_USERNAME=REPLACE_ME
DATABASE_PASSWORD=REPLACE_ME
JWT_SECRET=REPLACE_ME_MIN_32_CHARS
RABBITMQ_USERNAME=REPLACE_ME
RABBITMQ_PASSWORD=REPLACE_ME
